use std::{collections::HashMap, hash::Hash};

pub const EAT_MESSAGES: (&str, &str) = (
    "**do be eating tho...**", 
    "**is eating**"
);

pub const SLEEP_MESSAGES: (&str, &str) = (
    "**wants a nap.**",
    "**falls asleep.**"
);
